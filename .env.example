# Quant Lab Environment Configuration

# MLflow tracking URI (default: local file store)
MLFLOW_TRACKING_URI=./outputs/mlruns

# Weights & Biases (optional, Phase 6+)
# WANDB_API_KEY=your_key_here
# WANDB_PROJECT=quant_lab

# Device override (auto-detected by default)
# QUANT_LAB_DEVICE=cuda:0
